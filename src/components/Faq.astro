---
import type { FaqItem } from '../types';
import Icon from './Icon.astro';

interface Props {
  items: FaqItem[];
  openFirst?: boolean;
}

const { items, openFirst = true } = Astro.props;
---

<div class="faq-list border-t border-line">
  {
    items.map((item, index) => (
      <details class="faq-item border-b border-line" open={openFirst && index === 0}>
        <summary>
          <h3 class="faq-q m-0 pr-3">{item.q}</h3>
          <Icon name="chevron-down" size={18} class="faq-chevron text-brand" />
        </summary>
        <p class="body-16 text-ink-soft m-0 pb-4 pr-8">{item.a}</p>
      </details>
    ))
  }
</div>
