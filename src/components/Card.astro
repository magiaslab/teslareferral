---
type Props = {
  class?: string;
  tag?: 'div' | 'article' | 'section' | 'aside' | 'nav';
};

const { class: className = '', tag: Tag = 'div' }: Props = Astro.props;
---

<Tag class:list={['bg-surface border border-line rounded-[14px] p-7 shadow-sm', className]}>
  <slot />
</Tag>
